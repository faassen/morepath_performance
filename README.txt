== Morepath performance tracker ==

I measure Morepath performance using `welcome-bench`_ on my desktop
once every while (using Python 2). I keep the data in
``morepath_performance.txt``. With ``tojs.py`` and ``index.html`` I've
hacked up a way to generate a chart out of it.
